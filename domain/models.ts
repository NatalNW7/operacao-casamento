export interface User {
    name: string
    whatsapp: string
    email: string
    cpf: string
}

export interface Product {
    id: string
    name: string
    bonus: number
    quantity: number
    price: number
    total: number
}